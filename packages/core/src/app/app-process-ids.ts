/**
 * Default process IDs for the application
 */
export enum AppProcessIDs {
  /**
   * Main process
   */
  MAIN = 'main',
  /**
   * Main App window
   */
  APP = 'app',
  /**
   * Worker Process
   */
  WORKER = 'worker',
  /**
   * Sync Process
   */
  SYNC = 'sync',
}
