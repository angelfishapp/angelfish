import type { Theme } from '@mui/material/styles'
import makeStyles from '@mui/styles/makeStyles'

/**
 * CountryField Component Styles
 */

export const useStyles = makeStyles<Theme>(() => ({
  flag: {
    width: 20,
    marginRight: 10,
  },
}))
