import { styled } from '@mui/material/styles'

/**
 * ImportTransactions Component Styles
 */
export const MapperHeading = styled('h3')(({ theme }) => ({
  margin: 0,
  height: 45,
  color: theme.palette.primary.main,
}))
